Flask>=3.0,<4.0
python-dotenv>=1.0,<2.0

# Google Sheets (PROMPT 6). Импортируются лениво внутри
# services/google_sheets.py, поэтому их отсутствие не ломает
# остальное приложение (используется локальный резервный журнал).
gspread>=6.0,<7.0
google-auth>=2.0,<3.0

